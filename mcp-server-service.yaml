apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: todo-chatbot
  labels:
    app: mcp-server
spec:
  selector:
    app: mcp-server
  ports:
    - protocol: TCP
      port: 8002
      targetPort: 8002
  type: ClusterIP