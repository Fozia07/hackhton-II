---
id: 7
title: "mcp-tools-planning"
stage: "plan"
date_iso: "2026-01-19"
surface: "agent"
model: "Claude Sonnet 4.5"
feature: "todo-ai-chatbot"
branch: "002-mcp-tools-spec"
user: "Fozia"
command: "/sp.plan"
labels: ["planning", "mcp-tools", "phase-iii", "specification"]
links:
  spec: "specs/002-mcp-tools-spec/spec.md"
  plan: "specs/002-mcp-tools-spec/plan.md"
  tasks: "specs/002-mcp-tools-spec/tasks.md"
  ticket: null
  adr: null
  pr: null
files_yaml: |
  - specs/002-mcp-tools-spec/plan.md
  - specs/002-mcp-tools-spec/tasks.md
tests_yaml: |
  - specs/002-mcp-tools-spec/plan.md
  - specs/002-mcp-tools-spec/tasks.md
prompt_text: |
  # Todo AI Chatbot - MCP Tools Implementation Plan

  ## 1. Constitution Reinforcement

  **Core Isolation Principles:**
  - **Complete Isolation**: Phase III must remain completely isolated from Phase II - no modifications to Phase II code, files, or structures.
  - **READ-ONLY Phase II**: Phase II frontend & backend folders are strictly read-only - no modifications, overwrites, refactors, or direct imports allowed.
  - **Separation of Concerns**: Phase III operates in separate top-level folders within the same monorepo.
  - **No Manual Coding**: All code must be generated by Claude Code based on specs, plans, and tasks.

  **Step 2-Specific Rules:**
  - **Pure Functions**: MCP tools MUST be pure functions (no side effects outside DB) - they accept input, perform database operations, and return results.
  - **No Direct Imports**: No direct FastAPI/MCP SDK imports in tool functions - only database operations and validation.
  - **Exact Format Compliance**: Tools MUST match EXACT input/output formats from requirements (JSON examples) - no deviations.
  - **Database Scoping**: All DB access via SQLModel session, scoped to user_id for multi-tenancy.
  - **Stateless Operations**: Tools maintain no internal state - all state persisted in database.
  - **Error Consistency**: All errors handled consistently with standardized error responses.
  - **Async Compatibility**: Use async SQLModel sessions to align with FastAPI's async nature.

  ## 2. Step 2 Overview & Goals

  **Primary Objectives:**
  - **MCP Tools Implementation**: Create the 5 required MCP tools (add_task, list_tasks, complete_task, delete_task, update_task) with exact specifications.
  - **Database Integration**: Connect tools to the existing SQLModel database foundation with proper user_id scoping.
  - **Error Handling**: Implement comprehensive error handling with standardized responses.
  - **Validation**: Ensure all input parameters are validated according to specifications.
  - **Testing**: Create comprehensive tests for all MCP tools.

  **Expected Outcomes:**
  - Five fully functional MCP tools matching the exact specifications.
  - Proper integration with existing database models and session management.
  - Comprehensive error handling and validation.
  - Test coverage for all tools and edge cases.
  - Ready for MCP SDK wrapping and OpenAI Agent integration.

  ## 3. Dependencies & Prerequisites

  **Technical Prerequisites:**
  - Phase III database foundation completed (models, session management, utilities).
  - SQLModel and database connection properly configured.
  - Existing Task model and database utilities available.
  - Async database session management working.
  - Alembic migrations applied to database.

  **Phase II Assumptions:**
  - Database schema for Task exists conceptually (e.g., with user_id); Phase III will reuse the idea but implement its own access.
  - Database connection is stable and accessible.

  **Infrastructure Requirements:**
  - Neon PostgreSQL database connectivity.
  - Access to database models and session management utilities.
  - Testing environment with test database (e.g., local SQLite for unit tests).

  ## 4. Detailed Task Breakdown

  ### Task 2.1: Create MCP Tools Module Structure
  - **Description**: Set up the directory structure and base files for MCP tools implementation.
  - **Inputs needed**: MCP tools specification document.
  - **Outputs/Files to generate**:
    - `phase3-backend/app/mcp_tools/__init__.py`
    - `phase3-backend/app/mcp_tools/base.py` (base classes and utilities)
    - `phase3-backend/app/mcp_tools/__init__.py` (module exports)
  - **Dependencies**: None (initial task).
  - **Estimated complexity**: Low.
  - **Claude prompt hint**: "Create the MCP tools module structure with base classes and initialization files."

  ### Task 2.2: Implement add_task Tool
  - **Description**: Create the add_task MCP tool according to the specification.
  - **Inputs needed**: Task model from database foundation, specification for add_task.
  - **Outputs/Files to generate**:
    - `phase3-backend/app/mcp_tools/add_task.py` (implementation of add_task tool).
  - **Dependencies**: Task 2.1, Database foundation.
  - **Estimated complexity**: Medium.
  - **Claude prompt hint**: "Implement the add_task MCP tool with proper validation, database operations, and exact output format as specified."

  ### Task 2.3: Implement list_tasks Tool
  - **Description**: Create the list_tasks MCP tool according to the specification.
  - **Inputs needed**: Task model from database foundation, specification for list_tasks.
  - **Outputs/Files to generate**:
    - `phase3-backend/app/mcp_tools/list_tasks.py` (implementation of list_tasks tool).
  - **Dependencies**: Task 2.1, Database foundation.
  - **Estimated complexity**: Medium.
  - **Claude prompt hint**: "Implement the list_tasks MCP tool with optional status filtering, proper validation, and exact output format as specified."

  ### Task 2.4: Implement complete_task Tool
  - **Description**: Create the complete_task MCP tool according to the specification.
  - **Inputs needed**: Task model from database foundation, specification for complete_task.
  - **Outputs/Files to generate**:
    - `phase3-backend/app/mcp_tools/complete_task.py` (implementation of complete_task tool).
  - **Dependencies**: Task 2.1, Database foundation.
  - **Estimated complexity**: Medium.
  - **Claude prompt hint**: "Implement the complete_task MCP tool with proper validation, status updates, and exact output format as specified."

  ### Task 2.5: Implement delete_task Tool
  - **Description**: Create the delete_task MCP tool according to the specification.
  - **Inputs needed**: Task model from database foundation, specification for delete_task.
  - **Outputs/Files to generate**:
    - `phase3-backend/app/mcp_tools/delete_task.py` (implementation of delete_task tool).
  - **Dependencies**: Task 2.1, Database foundation.
  - **Estimated complexity**: Medium.
  - **Claude prompt hint**: "Implement the delete_task MCP tool with proper validation, deletion operations, and exact output format as specified."

  ### Task 2.6: Implement update_task Tool
  - **Description**: Create the update_task MCP tool according to the specification.
  - **Inputs needed**: Task model from database foundation, specification for update_task.
  - **Outputs/Files to generate**:
    - `phase3-backend/app/mcp_tools/update_task.py` (implementation of update_task tool).
  - **Dependencies**: Task 2.1, Database foundation.
  - **Estimated complexity**: Medium.
  - **Claude prompt hint**: "Implement the update_task MCP tool with proper validation, selective updates, and exact output format as specified."

  ### Task 2.7: Create MCP Tools Service Layer
  - **Description**: Create a lightweight service layer that coordinates all MCP tools with unified error handling (keep simple; no heavy orchestration).
  - **Inputs needed**: All individual tool implementations.
  - **Outputs/Files to generate**:
    - `phase3-backend/app/mcp_tools/service.py` (service layer coordinating all tools).
    - `phase3-backend/app/mcp_tools/exceptions.py` (custom exception classes).
  - **Dependencies**: Tasks 2.2-2.6.
  - **Estimated complexity**: Medium.
  - **Claude prompt hint**: "Create a lightweight service layer that coordinates all MCP tools with unified error handling and validation."

  ### Task 2.8: Create MCP Tools Tests
  - **Description**: Develop comprehensive tests for all MCP tools and service layer using pytest.
  - **Inputs needed**: All MCP tool implementations, service layer.
  - **Outputs/Files to generate**:
    - `phase3-backend/tests/test_mcp_tools.py` (tests for all MCP tools).
    - `phase3-backend/tests/test_mcp_service.py` (tests for service layer).
  - **Dependencies**: Tasks 2.2-2.7.
  - **Estimated complexity**: Medium.
  - **Claude prompt hint**: "Create comprehensive pytest tests for all MCP tools covering normal operations and edge cases, using a test DB."

  ## 5. Risks & Mitigations

  ### Risk 1: Database Transaction Issues
  - **Issue**: MCP tools performing database operations might have transaction consistency issues.
  - **Mitigation**: Use proper async session management with transaction handling, implement retry mechanisms for transient failures, and ensure all operations are atomic.

  ### Risk 2: Input Validation Inconsistencies
  - **Issue**: Different MCP tools might have inconsistent validation approaches.
  - **Mitigation**: Create a centralized validation module that all tools use, with standardized error responses and validation rules.

  ### Risk 3: User Isolation Failures
  - **Issue**: Tools might accidentally access or modify tasks belonging to other users.
  - **Mitigation**: Implement strict user_id scoping in all database queries, with comprehensive validation that each operation is performed on user-owned resources. Use Better Auth in Phase III for user_id validation.

  ### Risk 4: MCP SDK Integration Challenges
  - **Issue**: The tools might not integrate smoothly with the MCP SDK due to format mismatches.
  - **Mitigation**: Ensure exact compliance with the specified input/output formats, and create adapter functions if needed to bridge any format differences.

  ### Risk 5: Performance Issues with Large Task Lists
  - **Issue**: The list_tasks tool might be slow when users have many tasks.
  - **Mitigation**: Implement proper database indexing, add optional pagination for large result sets, and optimize database queries with proper select statements.

  ## 6. Success Criteria for Step 2
  - All 5 MCP tools implemented as pure functions with exact I/O formats.
  - Tools successfully interact with DB (e.g., add/list/complete a task via manual test).
  - Errors handled gracefully (e.g., task not found returns {"status": "error", "message": "..."}).
  - Tests pass with pytest (coverage for normal + edge cases).
  - No state held in memory; all ops stateless and DB-scoped.
  - Integration-ready for Step 3 (MCP Server).

  ## 7. Next Actions
  - Immediate next: Implement Task 2.1 (module structure) to bootstrap the folder.
  - Suggest prompt for first implementation task: "Based on this plan and previous spec, generate the code for Task 2.1: MCP tools module structure, including __init__.py and base.py in phase3-backend/app/mcp_tools/. Ensure async SQLModel compatibility and exact compliance with rules."
  - After all tasks: Review full module, then move to Step 3 spec/plan (MCP Server).
response_text: |
  Created comprehensive implementation plan and task breakdown for MCP tools:
  - Detailed plan with constitution reinforcement, goals, dependencies, and risks
  - 8 specific tasks with detailed descriptions, dependencies, and acceptance criteria
  - Proper sequencing from module structure to final testing
  - Clear next actions for implementation
outcome: "MCP tools implementation plan completed successfully with detailed task breakdown"
evaluation: "Complete plan with 8 tasks, dependencies, and acceptance criteria defined"
---
# Todo AI Chatbot - MCP Tools Implementation Plan

## 1. Constitution Reinforcement

**Core Isolation Principles:**
- **Complete Isolation**: Phase III must remain completely isolated from Phase II - no modifications to Phase II code, files, or structures
- **READ-ONLY Phase II**: Phase II frontend & backend folders are strictly read-only - no modifications, overwrites, refactors, or direct imports allowed
- **Separation of Concerns**: Phase III operates in separate top-level folders within the same monorepo
- **No Manual Coding**: All code must be generated by Claude Code based on specs, plans, and tasks

**Step 2-Specific Rules:**
- **Pure Functions**: MCP tools MUST be pure functions (no side effects outside DB) - they accept input, perform database operations, and return results
- **No Direct Imports**: No direct FastAPI/MCP SDK imports in tool functions - only database operations and validation
- **Exact Format Compliance**: Tools MUST match EXACT input/output formats from requirements (JSON examples) - no deviations
- **Database Scoping**: All DB access via SQLModel session, scoped to user_id for multi-tenancy
- **Stateless Operations**: Tools maintain no internal state - all state persisted in database
- **Error Consistency**: All errors handled consistently with standardized error responses
- **Async Compatibility**: Use async SQLModel sessions to align with FastAPI's async nature

## 2. Step 2 Overview & Goals

**Primary Objectives:**
- **MCP Tools Implementation**: Create the 5 required MCP tools (add_task, list_tasks, complete_task, delete_task, update_task) with exact specifications
- **Database Integration**: Connect tools to the existing SQLModel database foundation with proper user_id scoping
- **Error Handling**: Implement comprehensive error handling with standardized responses
- **Validation**: Ensure all input parameters are validated according to specifications
- **Testing**: Create comprehensive tests for all MCP tools

**Expected Outcomes:**
- Five fully functional MCP tools matching the exact specifications
- Proper integration with existing database models and session management
- Comprehensive error handling and validation
- Test coverage for all tools and edge cases
- Ready for MCP SDK wrapping and OpenAI Agent integration

## 3. Dependencies & Prerequisites

**Technical Prerequisites:**
- Phase III database foundation completed (models, session management, utilities)
- SQLModel and database connection properly configured
- Existing Task model and database utilities available
- Async database session management working
- Alembic migrations applied to database

**Phase II Assumptions:**
- Database schema for Task exists conceptually (e.g., with user_id); Phase III will reuse the idea but implement its own access
- Database connection is stable and accessible

**Infrastructure Requirements:**
- Neon PostgreSQL database connectivity
- Access to database models and session management utilities
- Testing environment with test database (e.g., local SQLite for unit tests)

## 4. Detailed Task Breakdown

### Task 2.1: Create MCP Tools Module Structure
- **Description**: Set up the directory structure and base files for MCP tools implementation
- **Inputs needed**: MCP tools specification document
- **Outputs/Files to generate**:
  - `phaseIII/backend/app/mcp_tools/__init__.py`
  - `phaseIII/backend/app/mcp_tools/base.py` (base classes and utilities)
- **Dependencies**: None (initial task)
- **Estimated complexity**: Low
- **Claude prompt hint**: "Create the MCP tools module structure with base classes and initialization files."

### Task 2.2: Implement add_task Tool
- **Description**: Create the add_task MCP tool according to the specification
- **Inputs needed**: Task model from database foundation, specification for add_task
- **Outputs/Files to generate**:
  - `phaseIII/backend/app/mcp_tools/add_task.py` (implementation of add_task tool)
- **Dependencies**: Task 2.1, Database foundation
- **Estimated complexity**: Medium
- **Claude prompt hint**: "Implement the add_task MCP tool with proper validation, database operations, and exact output format as specified."

### Task 2.3: Implement list_tasks Tool
- **Description**: Create the list_tasks MCP tool according to the specification
- **Inputs needed**: Task model from database foundation, specification for list_tasks
- **Outputs/Files to generate**:
  - `phaseIII/backend/app/mcp_tools/list_tasks.py` (implementation of list_tasks tool)
- **Dependencies**: Task 2.1, Database foundation
- **Estimated complexity**: Medium
- **Claude prompt hint**: "Implement the list_tasks MCP tool with optional status filtering, proper validation, and exact output format as specified."

### Task 2.4: Implement complete_task Tool
- **Description**: Create the complete_task MCP tool according to the specification
- **Inputs needed**: Task model from database foundation, specification for complete_task
- **Outputs/Files to generate**:
  - `phaseIII/backend/app/mcp_tools/complete_task.py` (implementation of complete_task tool)
- **Dependencies**: Task 2.1, Database foundation
- **Estimated complexity**: Medium
- **Claude prompt hint**: "Implement the complete_task MCP tool with proper validation, status updates, and exact output format as specified."

### Task 2.5: Implement delete_task Tool
- **Description**: Create the delete_task MCP tool according to the specification
- **Inputs needed**: Task model from database foundation, specification for delete_task
- **Outputs/Files to generate**:
  - `phaseIII/backend/app/mcp_tools/delete_task.py` (implementation of delete_task tool)
- **Dependencies**: Task 2.1, Database foundation
- **Estimated complexity**: Medium
- **Claude prompt hint**: "Implement the delete_task MCP tool with proper validation, deletion operations, and exact output format as specified."

### Task 2.6: Implement update_task Tool
- **Description**: Create the update_task MCP tool according to the specification
- **Inputs needed**: Task model from database foundation, specification for update_task
- **Outputs/Files to generate**:
  - `phaseIII/backend/app/mcp_tools/update_task.py` (implementation of update_task tool)
- **Dependencies**: Task 2.1, Database foundation
- **Estimated complexity**: Medium
- **Claude prompt hint**: "Implement the update_task MCP tool with proper validation, selective updates, and exact output format as specified."

### Task 2.7: Create MCP Tools Service Layer
- **Description**: Create a lightweight service layer that coordinates all MCP tools with unified error handling (keep simple; no heavy orchestration)
- **Inputs needed**: All individual tool implementations
- **Outputs/Files to generate**:
  - `phaseIII/backend/app/mcp_tools/service.py` (service layer coordinating all tools)
  - `phaseIII/backend/app/mcp_tools/exceptions.py` (custom exception classes)
- **Dependencies**: Tasks 2.2-2.6
- **Estimated complexity**: Medium
- **Claude prompt hint**: "Create a lightweight service layer that coordinates all MCP tools with unified error handling and validation."

### Task 2.8: Create MCP Tools Tests
- **Description**: Develop comprehensive tests for all MCP tools and service layer using pytest
- **Inputs needed**: All MCP tool implementations, service layer
- **Outputs/Files to generate**:
  - `phaseIII/backend/tests/test_mcp_tools.py` (tests for all MCP tools)
  - `phaseIII/backend/tests/test_mcp_service.py` (tests for service layer)
- **Dependencies**: Tasks 2.2-2.7
- **Estimated complexity**: Medium
- **Claude prompt hint**: "Create comprehensive pytest tests for all MCP tools covering normal operations and edge cases, using a test DB."

## 5. Risks & Mitigations

### Risk 1: Database Transaction Issues
- **Issue**: MCP tools performing database operations might have transaction consistency issues
- **Mitigation**: Use proper async session management with transaction handling, implement retry mechanisms for transient failures, and ensure all operations are atomic.

### Risk 2: Input Validation Inconsistencies
- **Issue**: Different MCP tools might have inconsistent validation approaches
- **Mitigation**: Create a centralized validation module that all tools use, with standardized error responses and validation rules.

### Risk 3: User Isolation Failures
- **Issue**: Tools might accidentally access or modify tasks belonging to other users
- **Mitigation**: Implement strict user_id scoping in all database queries, with comprehensive validation that each operation is performed on user-owned resources. Use Better Auth in Phase III for user_id validation.

### Risk 4: MCP SDK Integration Challenges
- **Issue**: The tools might not integrate smoothly with the MCP SDK due to format mismatches
- **Mitigation**: Ensure exact compliance with the specified input/output formats, and create adapter functions if needed to bridge any format differences.

### Risk 5: Performance Issues with Large Task Lists
- **Issue**: The list_tasks tool might be slow when users have many tasks
- **Mitigation**: Implement proper database indexing, add optional pagination for large result sets, and optimize database queries with proper select statements.

## 6. Success Criteria for Step 2
- All 5 MCP tools implemented as pure functions with exact I/O formats
- Tools successfully interact with DB (e.g., add/list/complete a task via manual test)
- Errors handled gracefully (e.g., task not found returns {"status": "error", "message": "..."})
- Tests pass with pytest (coverage for normal + edge cases)
- No state held in memory; all ops stateless and DB-scoped
- Integration-ready for Step 3 (MCP Server)

## 7. Next Actions
- Immediate next: Implement Task 2.1 (module structure) to bootstrap the folder
- Suggest prompt for first implementation task: "Based on this plan and previous spec, generate the code for Task 2.1: MCP tools module structure, including __init__.py and base.py in phaseIII/backend/app/mcp_tools/. Ensure async SQLModel compatibility and exact compliance with rules."
- After all tasks: Review full module, then move to Step 3 spec/plan (MCP Server)